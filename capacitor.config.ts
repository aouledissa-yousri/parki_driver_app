import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'parki_agent_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
